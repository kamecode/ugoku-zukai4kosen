# うごく図解 for 高専 — 公開手順

サイト一式はこのフォルダ（`website/`）で完結しています。外部CDN・画像参照なし、合計約1MB。

## 構成

- `index.html` — ポータル（科目別・単元別の一覧。45本すべてにリンク済み）
- `handotai/` — 半導体工学のアニメ図解25本
- `kairo/` — 電子回路のアニメ・図解20本
- `CNAME` — GitHub Pages の独自ドメイン設定ファイル（`ugoku-zukai4kosen.com`）
- `_filename_map.tsv` — 元教材ファイル（リポジトリ内の日本語名）との対応表。**教材側のHTMLを更新したらこの表に従ってコピーし直す**

## 手順1：ドメイン取得

お名前.com・ムームードメイン・Cloudflare Registrar 等で `ugoku-zukai4kosen.com` を取得
（年1,500円前後。Cloudflare Registrar が原価で最安・更新料の値上げなし）。

## 手順2：GitHub Pages で公開

```bash
cd website
git init && git add -A && git commit -m "initial publish"
gh repo create ugoku-zukai4kosen --public --source=. --push
gh api repos/{owner}/ugoku-zukai4kosen/pages -X POST \
  -f "source[branch]=master" -f "source[path]=/"
```

→ まず `https://<ユーザー名>.github.io/ugoku-zukai4kosen/` で公開されます。

## 手順3：独自ドメインを接続

1. GitHub のリポジトリ → Settings → Pages → Custom domain に `ugoku-zukai4kosen.com` を入力
   （`CNAME` ファイルを置いてあるので、push済みなら自動認識されます）
2. レジストラのDNS設定で以下を追加：
   - `A` レコード（@）→ `185.199.108.153` / `185.199.109.153` / `185.199.110.153` / `185.199.111.153` の4つ
   - `CNAME` レコード（www）→ `<ユーザー名>.github.io`
3. 反映後（数分〜数時間）、GitHub Pages 設定で **Enforce HTTPS** にチェック

## 更新のしかた

教材側（`semi-eng/図/`・`elec-cir-ex/図/`）のHTMLを直したら：

```bash
cd /home/hkame/lecture
while IFS=$'\t' read -r src dst; do cp "$src" "website/$dst"; done < website/_filename_map.tsv
cd website && git add -A && git commit -m "update" && git push
```

## 注意

- 気象庁の生データHTML（info2）は教材アニメではないので収録していません
- 経産省等の現物図はHTMLアニメには含まれていないため権利面の問題はありません（全て自作SVG/JS）
